name: Legacy release workflow disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Use the Publish immutable release workflow."
